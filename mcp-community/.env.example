# MCP2 커뮤니티 검색 서버 환경변수 (실제 값은 .env에 — git 무시됨)
# 사용: `cp .env.example .env` 후 값 채우기

COMMUNITY_DB_HOST=10.10.1.193
COMMUNITY_DB_PORT=3306
COMMUNITY_DB_USER=community_user
COMMUNITY_DB_PASSWORD=sbcommunity1234
COMMUNITY_DB_NAME=community_db

# TODO(운영 전환): 읽기 전용 mcp_reader 계정 발급 후 USER/PASSWORD만 교체.
#                 SELECT 권한만 부여 — MCP는 INSERT/UPDATE 하지 않음.
