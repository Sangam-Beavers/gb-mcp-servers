# 환율 자동 갱신 — Redis 접속 정보
#
# mcp-exchange / wallet-service 와 같은 Redis 인스턴스를 가리킨다.
# 실제 값은 .env 파일에 박고, .env 는 .gitignore 처리.
# .env.example 만 커밋해서 신규 환경 셋업 시 가이드로 활용.

REDIS_HOST=10.10.1.194
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0
